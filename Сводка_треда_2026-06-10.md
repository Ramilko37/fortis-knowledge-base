# Сводка треда (2026-06-10)

- Согласовали и реализовали переход источника правды для размещений на карте на `DefenseProject`:
  - пользов. размещения теперь идут через `placeObject` / `deletePlacedObject` и `placedObjects`.
  - карта/список/калькулятор/отчёт получают данные через `placedObjectsToMapPlacements(project)`.
  - `useDefenseStudioStore` оставлен только для runtime/3D-пути и не используется как расчетная модель.
- Обновили документацию проекта:
  - добавлен/актуализирован [[Продуктовый_план_Fortis]] с P0/P1/P2.
  - добавлен ADR-0005 о едином источнике размещений в KB.
  - `00_Index.md` теперь ссылается на roadmap.
  - в `AGENTS.md` добавлено правило: перед началом любой задачи проверять roadmap `knowledge-base/Продуктовый_план_Fortis.md`.
- В родительском и дочерних репозиториях выполнены коммиты и push по этим изменениям:
  - `frontend`: `65ce3f0`, `65ce3f0`-блок по prototype flow.
  - `knowledge-base`: `6a0a348`, `1dee047`, затем `b9b730a` для pointer update и новые записи roadmap.
  - `workspace`: `c1ba459`, `b9b730a`/`c1ba459`.
- Текущий статус: P0-сквозная логика уже частично закрыта, но требуется дополнительное доработки и очистка baseline (`build/lint`):
  - `defense-project.test.ts` и `pnpm lint` содержат ранее существующие ошибки вне затронутого scope.
  - по roadmap: в фокусе P0 — довести полностью sync карта ↔ список ↔ calculator ↔ отчет с «чистыми» локальными проверками.
- Условия запуска проверки, которые оставлены как следствие:
  - `npx tsx ...` контрактные тесты по placement/project/calculator успешно проверены по обновлённым сценариям.
  - требуется продолжить по плану: production build, focused lint по `/prototype` и `/calculator`, smoke на `/prototype` + `/calculator`.

## Дополнение: оптимизация `/prototype` (2026-06-10)

- Цель треда: оптимизация производительности `/prototype` после удаления legacy `/models` без изменения продуктовой логики.
- Результат: выполнен полный performance-блок для главного экрана.
- Изменения:
  - удалены eager `useGLTF.preload(...)` для `*.glb` из `src/modules/drone-defense/ui/scene.tsx`;
  - добавлены lightweight `previewImageUrl` для иконок эшелонов и переведены карточки/drag-ghost на миниатюры;
  - полноразмерные `imageUrl` оставлены как canonical ассеты;
  - добавлены thumbnails в `public/drone-defense/echelons/thumbs/*.avif` (`~1-2KB` каждый);
  - добавлены/обновлены контракты: `echelon-build-assets-contract`, `compact-asset-cards-contract`, `scene-contract`;
  - добавлены headers в `next.config.ts` для `/models/:path*` и `/drone-defense/:path*` с `Cache-Control: public, max-age=31536000, immutable`.
- Проверки:
  - `node test/route-contract.test.mjs` (legacy `/models` route отсутствует) — PASS;
  - `npx tsx src/modules/drone-defense/domain/echelon-build-assets-contract.test.ts` — PASS;
  - `node src/modules/drone-defense/domain/compact-asset-cards-contract.test.mjs` — PASS;
  - `node src/modules/drone-defense/domain/scene-contract.test.mjs` — PASS;
  - `pnpm build` — PASS;
  - cold transfer `/prototype` (1440x900) — `1.34 MB` (`< 10 MB`).
- Коммиты:
  - `frontend`: `3e4b40d` — Optimize prototype assets and lazy-load GLB models;
  - parent (`Fortis`): `52fbbad` — Update frontend submodule for prototype performance optimization.
