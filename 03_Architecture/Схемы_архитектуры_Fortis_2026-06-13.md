# Схемы архитектуры Fortis

Дата: 2026-06-13

Статус: обзор текущего workspace после `git fetch --all --prune` в parent, `frontend`, `backend`, `knowledge-base`.

Важно:

- Parent repo находится на `develop` и не отстаёт от `origin/develop`.
- `frontend` находится на `develop`, не отстаёт от `origin/develop`, но имеет локальные незакоммиченные изменения.
- `backend` в текущем checkout находится на `develop -> origin/develop` и содержит только шаблонный README.
- В `backend/origin/dev` есть актуальный Go backend с DDD-модулями, Postgres/GORM, probes/metrics и CRUD для `DefenseProject`/`Enterprise`.
- Parent repo сейчас показывает изменённый submodule pointer для `backend`: в parent записан `4041b12`, рабочий `backend/` стоит на `e2c4732`.

## 1. Карта репозиториев

```mermaid
flowchart TB
  W["fortis-workspace / parent repo"] --> WI["Workspace instructions / AGENTS.md"]
  W --> KB["knowledge-base / Obsidian notes"]
  W --> FE["frontend / Next.js app"]
  W --> BE["backend / Go API submodule"]
  W --> LEGACY["Fortis / legacy local vault, outside parent git history"]

  KB --> ROADMAP["Продуктовый план Fortis"]
  KB --> ADR["Architecture ADR"]

  FE --> FEAPP["src/app: Next.js App Router"]
  FE --> FEMOD["src/modules: product slices"]
  FE --> FESHARED["src/shared: types, stores, config, UI primitives"]

  BE --> BECURRENT["current checkout develop: placeholder"]
  BE --> BEDEV["origin/dev: active backend API"]
```

## 2. Текущая runtime-архитектура frontend

```mermaid
flowchart LR
  USER["Пользователь"] --> ROUTES["Next.js App Router"]

  ROUTES --> PUBLIC["/ login, register, landing"]
  ROUTES --> DASH["/dashboard: cabinet modules"]
  ROUTES --> PROTO["/prototype: GIS-конструктор"]
  ROUTES --> CALC["/calculator: расчёт и отчёт"]
  ROUTES --> NEXTAPI["/api/defense/*: local Next API routes"]

  PROTO --> PROJECT_STORE["useDefenseProjectStore"]
  PROTO --> STUDIO_STORE["useDefenseStudioStore"]
  PROTO --> GIS["GisBoard: MapLibre + Deck.gl"]
  PROTO --> DRILL["FacilityDrilldown: 3D / React Three Fiber"]

  CALC --> PROJECT_STORE
  CALC --> CALC_DOMAIN["calculator domain: costing, scoring, budget-fit"]
  CALC --> REPORT["CalculatorReport / browser print PDF"]

  PROJECT_STORE --> LS1["localStorage: fortis-defense-project"]
  STUDIO_STORE --> MOCK_RUNTIME["mock repository or /api/defense runtime"]
  NEXTAPI --> MOCK_REPO["mock-defense-repository"]
  MOCK_REPO --> MOCK_DATA["mock GIS data, facilities, catalog, layers"]
```

## 3. Главный источник истины DefenseProject

```mermaid
flowchart TB
  DP["DefenseProject"] --> BASE["baseObject: protected object + center"]
  DP --> LAYERS["layers: EditableDefenseLayer[]"]
  DP --> LIB["assetLibrary: DefenseAsset[]"]
  DP --> OBJECTS["placedObjects: PlacedDefenseObject[]"]
  DP --> STATE["activeLayerId, selectedAssetId, selectedObjectId, mode"]

  LAYERS --> GEOM["geometry: circle / ring / polygon / freeform"]
  LIB --> ASSET["asset metadata: category, roles, price, coverage, recommended layers"]
  LIB --> MOG["compoundProfile / protectionType for МОГ"]
  OBJECTS --> REL["assetId + layerId + coordinates + quantity + status"]
  OBJECTS --> CONFLICTS["geometry / coverage / terrain conflict flags"]

  REL --> RULE["Эшелон не хранит список средств"]
  RULE --> DERIVED["Состав эшелона = placedObjects filtered by layerId"]
```

## 4. Поток карта -> калькулятор -> отчёт

```mermaid
sequenceDiagram
  participant U as Пользователь
  participant Map as /prototype map UI
  participant Store as useDefenseProjectStore
  participant Project as DefenseProject
  participant Adapter as project-map-adapter
  participant Calc as /calculator
  participant Report as Print/PDF report

  U->>Map: выбирает эшелон и средство
  U->>Map: кликает по карте
  Map->>Store: placeObject(assetId, layerId, coordinates)
  Store->>Store: validateObjectPlacement()
  Store->>Project: добавляет PlacedDefenseObject
  Store->>Store: persist to localStorage
  Project->>Adapter: placedObjectsToMapPlacements()
  Adapter->>Map: markers / coverage / selected state
  Project->>Calc: projectToCalculatorConfiguration()
  Calc->>Calc: estimateConfiguration(), scoring, layer summaries
  Calc->>Report: CalculatorReport from same project-derived estimate
```

## 5. Модульные границы frontend

```mermaid
flowchart TB
  APP["src/app"] --> ROUTE_ONLY["thin route files"]
  APP --> SHELL["route layouts / DefenseStudioShell"]

  MODULES["src/modules"] --> DD_UI["drone-defense/ui"]
  MODULES --> DD_DOMAIN["drone-defense/domain"]
  MODULES --> DD_INFRA["drone-defense/infra"]
  MODULES --> CALC_UI["defense-calculator/ui"]
  MODULES --> CALC_DOMAIN["defense-calculator/domain"]
  MODULES --> CALC_INFRA["defense-calculator/infra"]

  SHARED["src/shared"] --> TYPES["types"]
  SHARED --> CONFIG["config: default layers, catalog mapping, asset library"]
  SHARED --> STORES["zustand stores"]
  SHARED --> HELPERS["pure helpers"]
  SHARED --> UI["shared UI primitives"]

  DD_UI --> DD_DOMAIN
  DD_DOMAIN --> DD_INFRA
  CALC_UI --> CALC_DOMAIN
  CALC_DOMAIN --> CALC_INFRA
  DD_UI --> SHARED
  CALC_UI --> SHARED

  SHARED -. "does not import modules" .-> MODULES
```

## 6. Исторические и текущие модели состояния

```mermaid
flowchart LR
  OLD["SelectedConfiguration / fortis-current-configuration"] --> MIGRATE["legacySelectedConfigurationToProject"]
  MIGRATE --> NEW["DefenseProject / fortis-defense-project"]

  NEW --> MAP["Map markers and echelon object list"]
  NEW --> CALC["Calculator live summary"]
  NEW --> REPORT["Report"]

  STUDIO["useDefenseStudioStore"] --> RUNTIME["mock/runtime scenario configuration"]
  STUDIO --> VIEW["view: gis / drilldown"]
  STUDIO --> LOCAL3D["local 3D placements"]

  RUNTIME -. "legacy/demo only" .-> MAP
  NEW -. "canonical user placements" .-> MAP
```

## 7. Backend `origin/dev` архитектура

```mermaid
flowchart TB
  HTTP["fasthttp router / cmd/app/router.go"] --> PROBES["/_/liveness, readiness, startup"]
  HTTP --> METRICS["/_/metrics Prometheus"]
  HTTP --> SWAGGER["/_/swagger, /_/swaggerui"]
  HTTP --> API["/api/v1"]

  API --> PROJECTS["projects controller"]
  API --> ENTERPRISES["enterprises controller"]
  API --> EXAMPLE["example/platform controller"]

  PROJECTS --> PROJECT_APP["defense_project application service"]
  PROJECT_APP --> PROJECT_DOMAIN["defense_project domain"]
  PROJECT_APP --> PROJECT_REPO["defense_project repository"]

  ENTERPRISES --> ENT_APP["enterprise application service"]
  ENT_APP --> ENT_DOMAIN["enterprise domain"]
  ENT_APP --> ENT_REPO["enterprise repository"]

  PROJECT_REPO --> GORM["GORM / rdbms wrapper"]
  ENT_REPO --> GORM
  GORM --> PG["PostgreSQL"]

  CONFIG["YAML config + APP_* env"] --> DI["dependencies.go DI"]
  DI --> HTTP
  DI --> GORM
```

## 8. Backend `origin/dev` API срез

```mermaid
flowchart LR
  CLIENT["Future frontend integration"] --> API["Go backend /api/v1"]

  API --> P1["POST /projects/import"]
  API --> P2["GET /projects/export?id="]
  API --> P3["POST /projects"]
  API --> P4["GET /projects?enterpriseId=&limit=&offset="]
  API --> P5["GET /projects/get?id="]
  API --> P6["PUT /projects/update?id="]
  API --> P7["DELETE /projects/delete?id="]

  API --> E1["POST /enterprises"]
  API --> E2["GET /enterprises"]
  API --> E3["PUT /enterprises"]
  API --> E4["DELETE /enterprises"]

  P1 --> JSONB["defense_projects.project_data JSONB"]
  P3 --> JSONB
  P4 --> JSONB
  P5 --> JSONB
  E1 --> ENT["enterprises table"]
  JSONB --> ENT_LINK["optional enterprise_id FK"]
  ENT_LINK --> ENT
```

## 9. Целевая интеграционная схема

```mermaid
flowchart LR
  FE["Frontend Defense Studio"] --> STORE["useDefenseProjectStore"]
  STORE --> LOCAL["localStorage fallback / draft cache"]
  STORE --> API_CLIENT["future backend project client"]

  API_CLIENT --> BACK["Go backend /api/v1/projects"]
  BACK --> SERVICE["DefenseProject service"]
  SERVICE --> VALIDATION["schemaVersion + project validation"]
  SERVICE --> DB["PostgreSQL JSONB snapshot"]

  FE --> MAP["/prototype map"]
  FE --> CALC["/calculator"]
  MAP --> STORE
  CALC --> STORE

  DB --> EXPORT["export JSON"]
  EXPORT --> STORE

  NOTE["Contract: backend should preserve DefenseProject schemaVersion=1"] --> SERVICE
```

## 10. Ключевые архитектурные факты

- MVP сфокусирован на 2D GIS-конструкторе: объект, карта, эшелоны, библиотека средств, размещение, покрытие, стоимость, отчёт.
- `DefenseProject.layers`, `DefenseProject.placedObjects`, `DefenseProject.assetLibrary` являются основной моделью пользовательской конфигурации.
- `/prototype` и `/calculator` уже связаны через один `DefenseProject`, а не через отдельные локальные состояния.
- `useDefenseStudioStore` остаётся для mock/runtime сценариев и 3D drill-down, но не должен становиться расчётным источником пользовательских размещений.
- Next API routes `/api/defense/*` сейчас обслуживают mock-данные frontend-прототипа.
- Реальный backend API уже виден в `backend/origin/dev`, но текущий рабочий checkout сабмодуля `backend` не переключён на эту ветку.
- Ближайшая интеграционная граница: заменить `localStorage` как единственный persistence слой на backend CRUD/import/export, сохранив схему `DefenseProject`.
