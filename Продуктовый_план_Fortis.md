# Продуктовый план Fortis: GIS MVP

**Дата актуализации:** 2026-07-23

**Последнее обновление:** 2026-07-24 — `/prototype` приведён к map-first модели: «Структура» и «Библиотека» взаимоисключающие, контекстный инспектор появляется только при выборе, а постоянный нижний обзор эшелонов и плавающая карточка объектов удалены. Карточки средств открываются в защищённом Fortis Drawer. Решение зафиксировано в [[03_Architecture/ADR-0014-map-first-gis-workspace|ADR-0014]].

## Scope decision

MVP Fortis по входящему ТЗ заказчика — это:

- **Модуль геоинформационной системы (ГИС) и построения цифровой модели защиты.**

Из MVP сознательно исключены:

- модуль ретроспективного анализа атак;
- модуль сценарного моделирования.

Эти направления не удаляются из долгосрочного видения, но не входят в ближайший roadmap и не должны влиять на P0/P1-приоритеты ГИС-модуля.

На 2026-06-17 добавлен клиентский WIP-модуль **`/retrospective-analysis`** в рамках `defense-studio`:
- демонстрационный расчёт цепочки «обнаружили → воздействовали → упало» для статических профилей БПЛА;
- derived-маркеры трассировки, оценка времени и простая plausibility-проверка по радиусу профиля;
- прототип остается вне критического пути GIS MVP и не связан с backend.

На 2026-06-16 старая 3D-сцена возвращена как отдельный вход **«Прототип Модуля сценарного моделирования»**. Это демонстрационный/исследовательский прототип будущего сценарного направления, а не основной рабочий контур GIS MVP и не источник расчётной конфигурации.

## Коротко

Fortis — это ГИС-конструктор защиты объекта от БПЛА. Пользователь собирает цифровую модель защиты на карте:

- объект → карта → эшелоны → библиотека средств → размещение → зоны покрытия → ограничения → стоимость → сравнение конфигураций → отчёт

Первый MVP строится вокруг 2D-карты. 3D остаётся целевым направлением цифровой модели, но не блокирует поставку 2D MVP.

Отдельный 3D-вход используется для демонстрации сценарного моделирования и локального drill-down объекта. В основном GIS-потоке пользователь продолжает работать через 2D-карту, эшелоны, библиотеку средств, калькулятор и отчёт.

Основной маршрут расчёта — `/calculator/`. Маршрут `/calculate/` не является критическим путём продукта.

## UI remediation после аудита 2026-07-10

Аудит подтвердил зрелую основу `/prototype`: реальная карта, связка карта ↔ дерево ↔ инспектор, зоны покрытия, стоимость и спокойный B2B-визуальный язык. Основные разрывы относительно профессиональных web-GIS находятся в управлении рабочим пространством и слоями, а не в декоративном оформлении.

Приоритет исправлений:

1. реально сворачиваемые панели и расширение карты при закрытии инспектора;
2. видимость эшелона, типа и отдельного объекта, плюс компактная легенда;
3. отсутствие неработающих map controls, рабочая линейка, basemap, home/reset и compass;
4. collision-aware подписи и actionable warnings;
5. понятная модель auto-save/undo/delete;
6. фасетная библиотека и объяснение совместимости;
7. desktop accessibility hardening и regression на `1280×720` и `1440×960`.

Отдельная мобильная версия `/prototype` и mobile bottom sheet исключены из текущего remediation scope. Уже реализованные responsive safeguards сохраняются, но дальнейшая mobile-функциональность не планируется в этой итерации.

Полный перечень недостатков и критерии выхода: [[05_Design/Prototype_GIS_UI_Audit_2026-07-10]]. Технический порядок реализации: `docs/superpowers/plans/2026-07-10-prototype-gis-ui-remediation.md` в parent workspace.

Повторная проверка после части remediation подтвердила, что collapsible panels и visibility уже работают, но сохранились семь незакрытых разрывов: безопасное удаление, hidden-active слой, картографическая графика и легенда, home/reset и измерение, search relevance/compatible-first, desktop accessibility и recovery при ошибках сохранения. Продуктовое поведение и границы Linear-задач зафиксированы в [[05_Design/GIS_UX_Hardening_Spec_2026-07-15]].

На 2026-07-17 эти семь разрывов закрыты реализацией в ветке `codex/gis-ux-hardening` и подтверждены unit/contract, keyboard, axe, viewport и local-backend тестами. Новые задачи FRT-133, FRT-134, FRT-135, FRT-136, FRT-137, FRT-138 и FRT-139 закрывают только отсутствовавший scope; существующие FRT-49, FRT-66, FRT-93, FRT-101, FRT-114 и FRT-115 сохраняют свою более широкую ответственность.

## Fortis UI Kit для GIS Workspace

На 2026-07-23 `/prototype` использует общий Fortis UI Kit как канонический runtime-слой: кнопки, поля, поиск, статусы, карточки, дерево, overlay-компоненты и семантические иконки совпадают со Storybook. MapLibre/deck.gl canvas, зоны покрытия, маркеры и азимутальные controls остаются специализированными GIS-компонентами, но наследуют Fortis tokens и actions. Подробный контракт и responsive-режимы описаны в [[03_Architecture/ADR-0013-gis-workspace-fortis-ui-kit-migration|ADR-0013]].

Мобильный режим не становится отдельным продуктовым flow: он сохраняет доступность карты и библиотеки как responsive safeguard. Основной производственный сценарий по-прежнему ориентирован на desktop GIS workspace.

## Map-first GIS Workspace

На 2026-07-24 главный экран `/prototype` перестроен так, чтобы карта занимала основной полезный объём desktop. Структура проекта и библиотека не конкурируют в одной левой панели; инспектор не имеет пустого постоянного состояния; legacy bottom drawer и отдельная карта объектов удалены. Карточка средства использует Fortis Drawer с inline validation, focus-management и подтверждением отмены несохранённых изменений. Контракт состояния и verification описаны в [[03_Architecture/ADR-0014-map-first-gis-workspace|ADR-0014]].

## МОГ как составной объект и демо-акцент

В рамках первого демо МОГ — главный показательный тип объекта, потому что в ТЗ вокруг него описаны люди, оснащение, оружие, сектор применения, ограничения по жилым зонам и стоимость.

МОГ **не является отдельным эшелоном**. Эшелоны отвечают за структуру защиты и дистанцию от объекта, а МОГ является типом средства/поста внутри подходящего эшелона, чаще всего в эшелонах огневого поражения или резерва.

Продуктовая модель МОГ:

- МОГ отображается как отдельный тип защиты и фильтр видимости на карте;
- МОГ размещается на карте как составной `PlacedDefenseObject`;
- карточка МОГ содержит состав группы, тип поста, подотчётность, оснащение, оружие, количество, азимут/сектор, дальность и стоимость;
- покрытие МОГ строится не от абстрактной группы, а от одного или нескольких видимых типов оружия внутри того же `PlacedDefenseObject`;
- один МОГ может одновременно показывать несколько покрытий с разными цветами;
- у каждого видимого покрытия МОГ свой азимут и свой сектор, которые настраиваются независимо на уровне оружия;
- в редакторе МОГ азимут и сектор на уровне оружия должны иметь визуальные circular controls рядом с точным числовым вводом градусов;
- стоимость МОГ считается как сумма состава, оружия и оснащения;
- отчёт должен показывать МОГ не как generic asset, а как пост с составом и вооружением.

См. [[03_Architecture/ADR-0009-mog-placement-instance-settings|ADR-0009]]: библиотека хранит шаблон МОГ, а верхний уровень настроек конкретного поста хранится в `PlacedDefenseObject.compoundProfile`.

Для демо на 2026-06-16 МОГ должен быть героем сценария: пользователь выбирает эшелон, размещает МОГ, задаёт минимальный состав/вооружение, видит сектор поражения, стоимость и попадание в отчёт.

Ограничение "МОГ нельзя стрелять в сторону жилых зон" фиксируется как важное P2-требование. В демо допускается показать warning-заготовку без полноценного географического conflict engine.

## Единая модель проекта

Единый источник правды для пользовательской конфигурации:

- `DefenseProject.layers`
- `DefenseProject.placedObjects`
- `DefenseProject.assetLibrary`

Любое размещение на карте должно становиться объектом `PlacedDefenseObject` в `DefenseProject`.

С 2026-06-17 эшелоны поддерживают две пользовательские формы: быстрый круговой/радиусный сценарий и произвольный `polygon`-контур, заданный точками на карте. Источник правды — `DefenseProject.layers[].geometry`; см. [[03_Architecture/ADR-0011-polygon-echelon-geometry|ADR-0011]].

Изменение объекта должно сразу отражаться:

- на карте;
- в списке объектов эшелона;
- в калькуляторе;
- в печатном отчёте.

`useDefenseStudioStore` остаётся для 3D/demo-runtime-слоёв, но **не** используется как основная модель расчётных размещений.

## P0 — закрыть сквозной 2D GIS-конструктор

Цель: пользователь должен собрать базовую конфигурацию защиты на карте и увидеть её в расчёте.

- [x] Перевести drag-to-map на `DefenseProject.placeObject`.
- [x] Убрать пользовательское употребление термина «слот» в UI в пользу «объект/карточка/позиция».
- [x] Исправить карточку объекта эшелона: показывать реальный эшелон, а не «Без эшелона».
- [x] Сделать объект управляемой сущностью:
  - выбор на карте;
  - отображение в списке;
  - отображение на карте;
  - удаление;
  - отображение количества, координат, стоимости, статуса.
- [x] Довести связку карта ↔ список:
  - клик по маркеру выбирает карточку;
  - клик по карточке выбирает маркер;
  - удаление убирает объект с карты и из расчёта.
- [ ] Калькулятор как live summary текущей карты (`/calculator/`):
  - стоимость по текущим объектам;
  - структура по эшелонам;
  - пустая конфигурация = пустой отчёт без старых бюджетных данных;
  - без НАК/НЕВ/ФОСФОРИТ/БМУ как центрального сценария.
- [ ] Базовый отчёт:
  - список размещённых объектов;
  - группировка по эшелонам;
  - стоимость по объектам и итого;
  - статус пустой конфигурации.
- [ ] Демо-сценарий МОГ к 2026-06-16:
  - [x] МОГ как готовая составная карточка библиотеки (через существующую `DefenseProject.assetLibrary`, без отдельного эшелона);
- [x] FRT-41: карточка МОГ содержит демо-поля (тип поста, личный состав, подотчётность, оружие, количество, дальность/сектор).
  - размещение МОГ в подходящем эшелоне на 2D-карте;
  - минимальная настройка состава: тип поста, личный состав, подотчётность, оружие, количество, азимут/сектор;
  - сектор поражения/действия на карте;
  - стоимость МОГ в калькуляторе и отчёте;
  - warning-заготовка по проверке сектора относительно жилой зоны без полноценного conflict engine.
- [ ] Verification baseline:
  - production build должен проходить;
  - focused lint по `/prototype` и `/calculator` должен быть чистым;
  - убрать debug side effects из `prototype/page.tsx`.

Критерий P0:

- Пользователь выбирает эшелон, размещает средство на 2D-карте, видит маркер и объект в панели эшелона, открывает `/calculator/` и видит актуальную стоимость и структуру этой же конфигурации.

## P1 — соответствие первому этапу ТЗ по ГИС-модулю

Цель: 2D MVP должен закрывать основные требования заказчика к модулю ГИС и цифровой модели защиты.

### Управление слоями и видимостью

- [ ] Слои по эшелонам:
  - обнаружение и классификация;
  - подавление;
  - огневое поражение;
  - огневое поражение, резерв.
- [ ] Слои по типам защиты:
  - системы обнаружения;
  - РЭБ;
  - МОГ;
  - огнестрельные установки, ЗАК/ЗРК/ПЗРК/ЗРПК и другие средства поражения;
  - сложные установки, состоящие из нескольких блоков.
- [ ] МОГ показывается как тип защиты/фильтр видимости, но не как отдельный эшелон.
- [ ] Управление видимостью:
  - видимость каждого эшелона;
  - видимость типа защиты;
  - видимость отдельной установки.

### Размещение и редактирование объектов

- [ ] Редактирование размещённого объекта:
  - количество;
  - координаты;
  - азимут/направление;
  - статус;
  - перенос между эшелонами.
- [ ] Размещение сложных установок:
  - обнаружение + РЭБ;
  - обнаружение + огневое поражение;
  - другие составные комплексы как единая карточка с несколькими зонами действия.
- [x] Сохранение конфигураций (backend: FRT-29, FRT-30, FRT-31, FRT-39 — 12.06.2026):
  - сохранить текущую карту;
  - загрузить ранее сохранённую конфигурацию;
  - назвать вариант конфигурации.
- [ ] UI сохранения/загрузки конфигураций (frontend).

### Покрытие

- [ ] Режим «Покрытие»:
  - показать/скрыть покрытие выбранного объекта;
  - показать/скрыть покрытие всех объектов;
  - не перегружать карту визуальным шумом.
- [ ] Поддержать формы покрытия:
  - круг;
  - сектор по азимуту;
  - зона/полигон;
  - отсутствие покрытия для нефизических объектов.

### Стоимость и бюджет

- [x] Считать общую стоимость конфигурации (backend: FRT-36 — 13.06.2026).
- [x] Считать стоимость по эшелонам и типам защиты (backend: FRT-36 — 13.06.2026).
- [x] Сравнивать стоимость разных сохранённых конфигураций (backend: FRT-37 — 15.06.2026).
- [x] Бюджетный лимит (backend: FRT-36 — 13.06.2026):
  - [x] режим «бюджет ограничен / не ограничен»;
  - [x] показывать остаток бюджета;
  - [x] не позволять добавить объект, если он превышает остаток бюджета, либо явно требовать подтверждение исключения, если заказчик выберет мягкий режим.

### Библиотека установок и карточки

- [x] Пользовательское добавление и редактирование средств в библиотеке (backend: FRT-33, frontend: FRT-48 — 13.06.2026).
- [ ] Базовые поля для всех карточек:
  - наименование;
  - тип;
  - пометка эшелона;
  - стоимость;
  - азимут/направление, если применимо;
  - дальность действия или обнаружения, если применимо;
  - мини-описание;
  - прикреплённые документы.
- [x] Поля МОГ (backend: FRT-34 — 13.06.2026):
  - количество личного состава;
  - тип поста: МОГ, ПВН, ГОР, КПП, другой пост;
  - подотчётность: Росгвардия, МО, ЧОП;
  - обмундирование и оснащение;
  - оружие и количество;
  - азимут/сектор применения;
  - дальность действия выбранного оружия;
  - суммарная стоимость состава, оружия и оснащения;
  - warning по необходимости проверки сектора относительно жилой зоны.
- [x] Поля оружия и установок (backend: FRT-34 — 13.06.2026):
  - калибр или боеприпас;
  - ручная/автоматическая работа;
  - дальность поражения;
  - количество модулей или единиц оружия на установку.
- [x] Поля систем обнаружения (backend: FRT-34 — 13.06.2026):
  - диапазон частот обнаружения;
  - дальность обнаружения;
  - активный/пассивный режим;
  - скорость оборота для активной РЛС;
  - поле зрения;
  - наличие тепловизора.
- [x] Поля РЭБ/спуферов (backend: FRT-34 — 13.06.2026):
  - диапазон частот;
  - дальность/зона действия;
  - азимут.

### Отчёт

- [x] Выгрузка отчёта по текущей конфигурации (backend: FRT-38 — 15.06.2026):
  - карта с расположением установок;
  - перечень установок по типу;
  - перечень установок по эшелону;
  - суммы по объектам, типам и итого;
  - основные ТТХ;
  - мини-описание;
  - режим скрытия стоимости, если стоимость не должна попадать в клиентский отчёт.

Критерий P1:

- Пользователь может собрать, сохранить и выгрузить 2D-карту защиты по требованиям первого этапа ТЗ: с эшелонами, типами средств, зонами покрытия, стоимостью, карточками установок и отчётом.

## P2 — ограничения, конфликты и географические барьеры

Цель: карта должна не только хранить объекты, но и подсвечивать ограничения применения.

- [ ] Модель препятствий:
  - жилые зоны;
  - леса;
  - высотность/рельеф;
  - застройка и другие барьеры.
- [ ] Ограничения применения:
  - МОГ нельзя стрелять в сторону жилых зон;
  - РЛС может работать в сторону жилых зон, если нет другого ограничения;
  - разные правила для разных типов средств.
- [ ] Conflict engine:
  - радиоэлектронная совместимость;
  - конфликт направлений/секторов;
  - дубль или перегрузка позиции;
  - объект вне допустимого эшелона;
  - превышение бюджета в hard-limit режиме.
- [ ] Отображение конфликтов:
  - на маркере;
  - в карточке объекта;
  - в панели эшелона;
  - в калькуляторе;
  - в отчёте.
- [ ] Terrain warnings:
  - предупреждение по объекту;
  - предупреждение по покрытию;
  - предупреждение в отчёте.

Критерий P2:

- Пользователь видит, почему конкретное размещение допустимо, рискованно или запрещено, и может исправить конфигурацию до выгрузки отчёта.

## P3 — цифровая 3D-модель защиты

Цель: развить 2D GIS MVP в многослойную 3D-карту объекта, не ломая `DefenseProject`.

- [ ] Согласовать, какие данные 2D-конфигурации должны переходить в 3D:
  - объект;
  - эшелоны;
  - размещённые установки;
  - зоны покрытия;
  - препятствия;
  - конфликты.
- [ ] Сделать адаптер `DefenseProject` → 3D scene/runtime.
- [ ] Перевести 3D drilldown из demo-runtime в режим просмотра текущей пользовательской конфигурации.
- [ ] Сохранить demo/cinematic режим отдельно от расчётной модели.

Критерий P3:

- 3D показывает ту же конфигурацию, что и 2D карта и калькулятор, а не отдельный сценарный mock.

## Future scope вне GIS MVP

Эти модули не входят в текущий MVP-roadmap:

- ретроспективный анализ атак;
- сценарное моделирование;
- библиотека профилей БПЛА для симуляций;
- вероятностные расчёты обнаружения/подавления/поражения;
- healthbar боевой устойчивости предприятия;
- анализ «точки невозврата».

Возвращаться к ним стоит только после закрытия P1 или отдельного решения заказчика о расширении scope.

## Проверочный сценарий GIS MVP

- открыть `/prototype/`;
- выбрать предприятие/объект;
- выбрать эшелон;
- выбрать МОГ в библиотеке средств;
- разместить МОГ на карте;
- настроить тип поста, личный состав, подотчётность, оружие, количество и азимут/сектор;
- проверить маркер и зону покрытия;
- открыть список объектов эшелона;
- проверить карточку объекта;
- изменить количество/координаты/азимут;
- включить/выключить видимость объекта, эшелона и типа защиты;
- перейти в `/calculator/`;
- проверить стоимость, структуру и бюджетный остаток;
- сформировать отчёт;
- очистить конфигурацию и проверить пустое состояние без старых бюджетных данных.

## Assumptions

- Ближайший MVP — frontend-first 2D GIS, без полноценного backend collaborative editing.
- Backend потребуется для пользовательских доступов, предприятий, совместного редактирования, документов карточек и durable server-side storage.
- 3D не является блокером первого MVP, но архитектура не должна мешать будущей цифровой 3D-модели.
- Бюджетный hard limit указан в ТЗ, но финальный UX может быть hard-block или soft-confirmation после согласования с заказчиком.
- Ретроспективный анализ и сценарное моделирование не участвуют в приоритизации P0/P1.
- МОГ является главным демо-акцентом первого GIS-среза, но не меняет модель эшелонов: это составной объект/тип защиты внутри `DefenseProject`.
- Локальная fallback-библиотека frontend может содержать `mock: true` ТТХ для демо-карты. Эти значения нужны для визуализации coverage и не являются справочником реальных характеристик средств; frontend хранит дальности и радиусы в метрах согласно [[03_Architecture/ADR-0008-asset-library-api-frontend-boundary|ADR-0008]].

---

## Changelog

### 2026-06-16 — Frontend: mock-ТТХ для покрытия библиотеки средств

Каноническая frontend fallback-библиотека `DefenseProject.assetLibrary` получает demo-ТТХ для первого отображения покрытия на карте:

- средства обнаружения получают `detectionSpec`;
- РЭБ/спуферы получают `ewSpec`;
- ПВО, ПЗРК/ЗРПК, МОГ и другие кинетические средства получают `weaponSpec`;
- `coverageRadius` и `coverageAngle` заполняются из mock-ТТХ, чтобы карта могла показывать круги и сектора покрытия без ожидания вендорских данных.

Значения помечены как mock/demo и должны быть заменены или подтверждены при появлении реальной базы характеристик.

### 2026-06-15 — Frontend: FRT-89 3D placeholder для демо

До полноценного P3-модуля вкладка `3D` в `/prototype` открывает отдельный state `/prototype?view=3d` с product placeholder:

- пользователь видит 3D-визуал и текст `Раздел в разработке`;
- основной рабочий контур остаётся 2D GIS-конструктором;
- legacy route `/models` не возвращается;
- текущая 2D-карта и `/calculator` не меняют модель данных и продолжают работать от `DefenseProject`.

### 2026-06-15 — Frontend: auth guard временно opt-in

Пока backend auth не поднят в окружениях, frontend не должен блокировать демо-экраны редиректом на login:

- `src/proxy.ts` включает redirect guard только при `FORTIS_AUTH_ENABLED=true`;
- без этого env `/prototype`, `/calculator` и `/workspace` открываются напрямую;
- same-origin auth routes и HttpOnly cookie flow остаются в коде для будущего включения backend-backed контура.

### 2026-06-15 — Backend: closed FRT-56 (update полного DefenseProject через projectJson + version)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-56](https://linear.app/fortis-project/issue/FRT-56) | BE: full `DefenseProject` update через `projectJson + version` | Done |
| [FRT-131](https://linear.app/fortis-project/issue/FRT-131) | FE: передавать version в теле PUT /api/v1/projects/update | Backlog |

**Что это даёт продукту:**
- `PUT /api/v1/projects/update` теперь поддерживает опциональное поле `version` для раннего обнаружения конфликта версий (optimistic locking).
- При несовпадении `version` backend возвращает `409 Conflict`.
- Если client не передаёт `version` — поведение не меняется (backward compatible).
- Поле `projectJson` позволяет frontend сохранять полный `DefenseProject` (layers, placedObjects, assetLibrary) одной операцией.
- 5 новых unit-тестов (application + controller), все проходят.
- PR [#15](https://github.com/we-are-komanda/fortis-back/pull/15) вмержен в develop.
- Frontend-тикет FRT-131 создан.

### 2026-06-15 — Backend: closed FRT-69 (backend-контур авторизованной зоны)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-69](https://linear.app/fortis-project/issue/FRT-69) | Backend-контур авторизованной зоны: пользователи, доступ к предприятиям и сохранение конфигураций | Done |
| [FRT-70](https://linear.app/fortis-project/issue/FRT-70) | FE: Auth guard и redirect для /prototype и /calculator | Todo (unblocked by FRT-69) |
| [FRT-71](https://linear.app/fortis-project/issue/FRT-71) | FE: Авторизованная рабочая зона проектов | Todo (unblocked by FRT-69) |
| [FRT-72](https://linear.app/fortis-project/issue/FRT-72) | FE: Current DefenseProject context между /prototype и /calculator | Todo (unblocked by FRT-69) |

**Что это даёт продукту:**
- Полный DDD-цикл модуля User (Domain → Infrastructure → Application → UI).
- JWT-аутентификация (register, login, me, token_validate).
- AuthRequired middleware с whitelist для публичных эндпоинтов.
- User-Enterprise связь: таблица user_enterprises, проверка доступа к предприятию.
- Фильтрация предприятий по пользователю: GET /api/v1/enterprises возвращает только доступные.
- Миграции 000009_users и 000010_user_enterprises.
- 32 unit-теста (все проходят с -race).
- PR [#14](https://github.com/we-are-komanda/fortis-back/pull/14) вмержен в develop.
- Frontend-тикеты FRT-70, FRT-71, FRT-72 разблокированы.

### 2026-06-15 — Backend: closed FRT-38 (API данных для отчёта GIS MVP)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-38](https://linear.app/fortis-project/issue/FRT-38) | API данных для отчёта GIS MVP | Done |
| [FRT-28](https://linear.app/fortis-project/issue/FRT-28) | FE: расширенный отчёт | Todo (unblocked by FRT-38) |

**Что это даёт продукту:**
- Новый endpoint `GET /api/v1/projects/report?id={id}&hideCost={bool}` — единый payload для отчёта GIS MVP.
- Ответ содержит: карту/объекты/слои/типы/стоимость/ТТХ/структурный профиль.
- Режим скрытия стоимости (hideCost).
- МОГ-строки (compositionSummary, weaponSummary, azimuthSectorSummary).
- 12 unit-тестов, 2052 строки кода.
- PR [#13](https://github.com/we-are-komanda/fortis-back/pull/13) вмержен в develop.

### 2026-06-15 — Backend: closed FRT-37 (API сравнения конфигураций)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-37](https://linear.app/fortis-project/issue/FRT-37) | API сравнения конфигураций | Done |
| [FRT-52](https://linear.app/fortis-project/issue/FRT-52) | FE: UI сравнения конфигураций (интеграция с FRT-37 API) | Backlog |

**Что это даёт продукту:**
- Новый endpoint `GET /api/v1/projects/compare?id1={id1}&id2={id2}` — A/B сравнение двух конфигураций проектов.
- Ответ содержит структурные профили (objectCount, unitCount, echelonCount, categoryCount, conflictCount, coveredObjCount) и стоимость обеих конфигураций с разбивкой по эшелонам и типам.
- Diff-секция с дельтой по каждому показателю.
- 22 новых unit-теста, 1430 строк кода.
- PR [#12](https://github.com/we-are-komanda/fortis-back/pull/12) вмержен в develop.

### 2026-06-15 — Frontend: FRT-74 first map screen

Для первого экрана `/prototype` зафиксировано UX-поведение демо-карты:

- карта открывается на `DefenseProject.baseObject.center`, а не на дальнем региональном эшелоне;
- первый viewport использует масштаб уровня промышленной площадки и ближайших эшелонов;
- на карте показываются центр объекта защиты и аккуратный demo placeholder-периметр вокруг него;
- scale bar считается от текущего zoom/latitude и не должен показывать hardcoded `1000 км`;
- touchpad / wheel zoom на карте должен быть заметно замедлен относительно дефолта Deck.gl (`scrollZoom.speed = 0.00125`), чтобы скролл не пролетал через несколько уровней масштаба;
- активный эшелон и размещения остаются в `DefenseProject`, без переноса источника правды в `useDefenseStudioStore`.

### 2026-06-17 — Frontend: basemap switching for `/prototype`

Для карты `/prototype` зафиксирована отдельная view-модель базовой подложки:

- basemap не входит в `DefenseProject.layers`, `placedObjects`, калькулятор, budget check или conflict engine;
- текущий источник карты хранится отдельно в `useMapViewStore` и восстанавливается из `localStorage`;
- список источников живёт в registry `src/shared/config/base-map-sources.ts`, а URL не хардкодятся внутри UI-компонента карты;
- MVP-источники: `openfreemap-bright`, `osm-standard`, `topographic`, архитектурная точка расширения `satellite-demo`, а также `internal-basemap` для закрытого контура;
- добавлены env-флаги `NEXT_PUBLIC_FORTIS_ALLOW_EXTERNAL_BASEMAPS` и `NEXT_PUBLIC_FORTIS_DEFAULT_BASEMAP`;
- для self-hosted/internal basemap поддерживаются env-настройки `NEXT_PUBLIC_FORTIS_INTERNAL_BASEMAP_STYLE_URL`, `NEXT_PUBLIC_FORTIS_INTERNAL_BASEMAP_TILES`, `NEXT_PUBLIC_FORTIS_INTERNAL_BASEMAP_TYPE`, `NEXT_PUBLIC_FORTIS_INTERNAL_BASEMAP_ATTRIBUTION`;
- для topo/satellite demo поддерживаются конфигурационные env `NEXT_PUBLIC_FORTIS_TOPOGRAPHIC_BASEMAP_TILES`, `NEXT_PUBLIC_FORTIS_TOPOGRAPHIC_BASEMAP_ATTRIBUTION`, `NEXT_PUBLIC_FORTIS_SATELLITE_BASEMAP_TILES`, `NEXT_PUBLIC_FORTIS_SATELLITE_BASEMAP_TYPE`, `NEXT_PUBLIC_FORTIS_SATELLITE_BASEMAP_ATTRIBUTION`;
- при `NEXT_PUBLIC_FORTIS_ALLOW_EXTERNAL_BASEMAPS=false` UI скрывает внешние источники и оставляет только `allowedInClosedContour=true`, без внешних запросов;
- при ошибке загрузки выбранного источника карта показывает warning и откатывается на допустимый default basemap без сброса пользовательских overlays и viewport.

### 2026-06-14 — GIS prototype UX polish

Для демо-сценария карты уточнено поведение интерфейса:

- двойной клик по размещённому средству на карте должен плавно приближать карту к этому средству и держать его в фокусе, повторный двойной клик не должен выполнять zoom out;
- карта должна иметь явные controls `+/-` в правом верхнем углу и блокировать browser page zoom внутри области карты;
- выбор объекта защиты переносится в компактный dropdown поверх карты;
- лишние секции `Объект защиты`, `Выбранная карточка` и отдельная кнопка поиска на карте убираются из первого демо-потока;
- sidebar библиотеки средств должен плавно сворачиваться и открываться, не ломая контекст карты.

### 2026-06-14 — Linear FE closing workflow

В Linear не используется отдельный статус `Blocked`, поэтому FE-блокировки оформляются через relations и workflow comments.

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-47](https://linear.app/fortis-project/issue/FRT-47) | FE: обработка version conflict | Todo, blocked by FRT-56 |
| [FRT-50](https://linear.app/fortis-project/issue/FRT-50) | FE: документы карточек средств через BE API | Todo |
| [FRT-51](https://linear.app/fortis-project/issue/FRT-51) | FE: cost/budget BE API integration | Todo |
| [FRT-56](https://linear.app/fortis-project/issue/FRT-56) | BE: full `DefenseProject` update через `projectJson + version` | Done |
| [FRT-19](https://linear.app/fortis-project/issue/FRT-19) | FE: сравнение сохранённых конфигураций | blocked by FRT-37 |
| [FRT-28](https://linear.app/fortis-project/issue/FRT-28) | FE: расширенный отчёт | blocked by FRT-38 |

Зафиксирован процесс: после закрытия backend parent-задачи связанная FE-задача переводится из `Backlog` в `Todo`, получает комментарий `Workflow update`, а реальные оставшиеся зависимости оформляются через Linear relations.

См. [[04_Process/Linear_FE_closing_workflow_2026-06-14]].

### 2026-06-13 — Backend: closed FRT-36 (API стоимости и бюджетных ограничений)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-36](https://linear.app/fortis-project/issue/FRT-36) | API стоимости и бюджетных ограничений | Done |
| [FRT-51](https://linear.app/fortis-project/issue/FRT-51) | FE: Интеграция бэкенд-API стоимости и бюджетных ограничений | Todo |

**Что это даёт продукту:**
- 4 новых API-эндпоинта для расчёта стоимости конфигурации и управления бюджетом.
- Расчёт стоимости с группировкой по объектам, эшелонам, типам и итого.
- Бюджетный лимит: limited/unlimited режимы, остаток бюджета, проверка добавления средств.
- Миграция `000008_budget_configs`, 19 unit-тестов, 2245 строк кода.

**Следующие шаги (BE):** FRT-37 (сравнение конфигураций), FRT-38 (отчёт).

### 2026-06-13 — Backend: closed FRT-35 (хранение документов карточек средств)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-35](https://linear.app/fortis-project/issue/FRT-35) | Хранение документов карточек средств защиты | Done |
| [FRT-50](https://linear.app/fortis-project/issue/FRT-50) | FE: Интеграция документов карточек с BE API | Todo |

**Что это даёт продукту:**
- Backend предоставляет CRUD-эндпоинты для документов, прикреплённых к карточкам средств защиты (`/api/v1/assets/documents/*`).
- 5 эндпоинтов: список, метаданные, download URL, создание, удаление.
- 33 unit-теста, все проходят.
- Создан FE-тикет FRT-50 для интеграции на фронте.

### 2026-06-13 — Backend: closed FRT-34 (детальные карточки установок)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-34](https://linear.app/fortis-project/issue/FRT-34) | Schema детальных карточек ТТХ для установок | Done |

**Что это даёт продукту:**
- Backend хранит все P1 поля карточек ТТХ: оружие/установки, системы обнаружения, РЭБ/спуферы.
- Добавлены Value Objects `WeaponSpecification`, `DetectionSpecification`, `EWSpecification` с валидацией spec↔category.
- МОГ расширен полями: тип поста, подотчётность, оснащение, оружие, сектор/дальность.
- 10 новых domain-тестов, все существующие проходят.

### 2026-06-13 — Backend: closed FRT-33 (библиотека средств защиты)

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-33](https://linear.app/fortis-project/issue/FRT-33) | API библиотеки средств защиты (CRUD) | Done |
| [FRT-48](https://linear.app/fortis-project/issue/FRT-48) | FE: Интеграция библиотеки с BE API и UI управления карточками | Done |

**Что это даёт продукту:**
- Backend предоставляет CRUD-эндпоинты для библиотеки средств защиты (`/api/v1/assets`).
- Разделение общего каталога (`is_public=true`) и пользовательских карточек (`enterprise_id`).
- JSONB-хранение для гибкой схемы, совместимой с `DefenseProject.assetLibrary`.
- Frontend `/prototype` загружает библиотеку через same-origin `/api/v1/assets`, а Next proxy направляет запросы на backend `FORTIS_API_BASE_URL` без `trailingSlash`-редиректа.
- UI управления библиотекой поддерживает создание, редактирование, удаление и fallback на локальную библиотеку при недоступном backend.
- Контракт единиц измерения зафиксирован: backend DTO хранит дальности в километрах, frontend `DefenseProject.assetLibrary` — в метрах.
- 22 unit-теста, lint/build — пройдены.

**Frontend verification FRT-48:**
- `pnpm exec tsx src/modules/drone-defense/infra/asset-api-proxy-contract.test.ts`
- `pnpm exec tsx src/modules/drone-defense/infra/asset-library-api.test.ts`
- `pnpm exec tsx src/shared/lib/use-defense-project-store.test.ts`
- `pnpm exec eslint ...`
- `pnpm build`
- browser `/prototype`: backend asset появляется в каталоге; при backend unavailable остаётся мягкий fallback.

**Следующие шаги (BE):** FRT-37 (сравнение), FRT-38 (отчёт).

### 2026-06-13 — FRT-32: версионирование конфигураций

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-32](https://linear.app/fortis-project/issue/FRT-32) | Версионирование конфигураций (optimistic locking) | Done |
| [FRT-47](https://linear.app/fortis-project/issue/FRT-47) | FE: Обработка version конфликта (дочерний FRT-32) | Todo |

**Что это даёт продукту:** параллельные правки конфигурации не перетирают друг друга. Backend возвращает `409 Conflict` при попытке сохранить устаревшую версию. Поле `version` в ответах API позволяет фронту отображать актуальность данных.

**Следующие шаги (BE):** FRT-34 (детальные карточки), FRT-35 (документы), FRT-36 (стоимость/бюджет), FRT-37 (сравнение), FRT-38 (отчёт).

### 2026-06-12 — GIS MVP changelog: closed 5 tickets

Реализован backend для durable GIS MVP. Закрыты тикеты:

| Тикет | Описание | Статус |
|---|---|---|
| [FRT-29](https://linear.app/fortis-project/issue/FRT-29) | Модель предприятий и защищаемых объектов | Done |
| [FRT-30](https://linear.app/fortis-project/issue/FRT-30) | Хранение DefenseProject | Done |
| [FRT-31](https://linear.app/fortis-project/issue/FRT-31) | API сохранения и загрузки конфигураций карты | Done |
| [FRT-39](https://linear.app/fortis-project/issue/FRT-39) | Import/export DefenseProject JSON | Done |
| [FRT-41](https://linear.app/fortis-project/issue/FRT-41) | МОГ как готовая составная карточка в библиотеке | Done |

**Что это даёт продукту:**
- Backend может хранить `DefenseProject` (слои, объекты, библиотеку) с версионированием.
- API возвращает список предприятий/объектов защиты; каждый проект может быть привязан к защищаемому объекту.
- CRUD для вариантов конфигурации: создание, получение, обновление, удаление.
- Импорт/экспорт JSON, совместимый с frontend-форматом.
- JSON, экспортированный frontend, можно импортировать backend-ом и наоборот.

**Следующие шаги (BE):** FRT-33 (библиотека средств), FRT-34 (детальные карточки), FRT-35 (документы), FRT-36 (стоимость/бюджет), FRT-37 (сравнение), FRT-38 (отчёт).
